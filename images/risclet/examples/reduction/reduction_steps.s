                .global reduction_steps
                .text

# reduction_steps(value) -> number of operations needed to reach zero
reduction_steps:
                # Write your code here.
                ret
